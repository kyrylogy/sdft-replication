# Tier-2 scale curve (baseline arm): classic SFT-LoRA at 14B, stage 1, Tool Use.
extends: [shared/model_14b.yaml]
experiment:
  name: sft_lora_14b_tooluse_s1
  notes: "Tier2 scale curve baseline. 14B LoRA SFT."
method:
  objective: sft
  tuning: lora
  teacher: none
train:
  learning_rate: 1.0e-4
data:
  dataset: tooluse
  stage: 1
